{
  "name": "portal-clientes",
  "private": true,
  "description": "Cloudflare Pages Functions do portal-clientes. Sem dependências — verificação de token do Clerk é feita na mão com crypto.subtle nativo (ver functions/_lib/clerk.js). Arquivo mantido vazio de propósito pra o build command 'npm install' configurado no painel continuar funcionando sem erro."
}
